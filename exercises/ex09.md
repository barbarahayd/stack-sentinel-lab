# Ex09 — Definir AgentState

## Contexto

O state é o contrato que atravessa os nodes do agente. Ele deve começar completo, mesmo que vários campos ainda estejam vazios.

## Arquivo permitido

```text
stack_sentinel/agent/state.py
```

## Contrato

Implemente:

```python
create_initial_state(user_input: str) -> AgentState
```

O retorno deve conter:

- `user_input` com o texto recebido;
- `intent`;
- `ticket_id`;
- `build_id`;
- `context`;
- `error`;
- `final_answer`.

Todos os campos exceto `user_input` devem começar com `None`.

## Validação

```bash
python run.py test ex09
```

