# Ex11 — Roteamento condicional

## Contexto

Agora o fluxo deixa de ser linear. A intenção classificada define qual node deve executar em seguida.

## Arquivo permitido

```text
stack_sentinel/agent/graph.py
```

## Contrato

Implemente:

```python
route_by_intent(state: AgentState) -> str
```

Mapeamento obrigatório:

- `ticket` -> `fetch_ticket`;
- `build` -> `fetch_build`;
- `docs` -> `fetch_docs`;
- qualquer outro valor -> `fallback`.

## Validação

```bash
python run.py test ex11
```

